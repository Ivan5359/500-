# RevenueSprint 48 deploy folder

Upload this whole folder to a new GitHub repository or make it the Railway project root.

Railway settings:

- Root directory: `RAILWAY_READY_REVENUESPRINT` if this folder is inside a bigger repo.
- Start command: `npm start`
- Healthcheck path: `/health`

After deploy open:

`https://YOUR-APP.up.railway.app/?v=clean-20260616`

If iPhone still shows an old version, delete the old Home Screen icon and open the link in a private tab once.
